< INPUT TYPE = BUTTON OnClick = "somefunction("
products.html ");return false"
VALUE = "somevalue" >

    $fn.somefunction = function (.. / layout / pages / products.html) {
        window.location = url;
    };
